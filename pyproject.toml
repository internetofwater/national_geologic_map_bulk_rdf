[project]
name = "national-geology-map-bulk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "geopandas>=1.1.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
